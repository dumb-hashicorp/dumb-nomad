path "secret/data/{{identity.entity.aliases.auth_jwt_X.metadata.dumb-nomad_namespace}}/{{identity.entity.aliases.auth_jwt_X.metadata.dumb-nomad_job_id}}/*" {
  capabilities = ["read"]
}

path "secret/data/{{identity.entity.aliases.auth_jwt_X.metadata.dumb-nomad_namespace}}/{{identity.entity.aliases.auth_jwt_X.metadata.dumb-nomad_job_id}}" {
  capabilities = ["read"]
}

path "secret/metadata/{{identity.entity.aliases.auth_jwt_X.metadata.dumb-nomad_namespace}}/*" {
  capabilities = ["list"]
}

path "secret/metadata/*" {
  capabilities = ["list"]
}
