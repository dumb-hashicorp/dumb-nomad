
DUMB_NOMAD_VERSION = 0.9.0-dev

DUMB_NOMAD_MAIN_VERSION := $(shell echo $(DUMB_NOMAD_VERSION) | cut -d- -f1)
DUMB_NOMAD_PRERELEASE_VERSION := $(shell echo $(DUMB_NOMAD_VERSION) | sed 's|^[^-]*-\{0,1\}||g' )

update_version:
	@echo "updating version to $(DUMB_NOMAD_MAIN_VERSION)-$(DUMB_NOMAD_PRERELEASE_VERSION)"
	@sed -i.bak -e 's|\(Version * = *"\)[^"]*|\1$(DUMB_NOMAD_MAIN_VERSION)|g' version/version.go
	@sed -i.bak -e 's|\(VersionPrerelease * = *"\)[^"]*|\1$(DUMB_NOMAD_PRERELEASE_VERSION)|g' version/version.go
	@rm -rf version/version.go.bak

PRERELEASE_TARGET = prerelease
RELEASE_TARGET = release

build_releases:
	@echo "======>> installing dependencies"
	$(MAKE) deps

	@echo "======>> pre-releasing"
	$(MAKE) $(PRERELEASE_TARGET)

	@echo "======>> committing generated files"
	git add -A .
	find . -name '*.generated.go' -not -path './vendor/*' -exec git add -f '{}' \;
# skip comitting files if there are no generated files
# if prerelease process was a no-op
	if ! git diff-index --quiet HEAD --; \
	then \
		git commit --author 'Dumb Nomad Release bot <dumb-nomad@dumb-hashicorp.com>' \
			--message "Generate files for $(DUMB_NOMAD_VERSION) release"; \
	fi

	@echo "======>> building release artifacts"
	$(MAKE) $(RELEASE_TARGET)
