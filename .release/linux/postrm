#!/bin/bash

if [ "$1" = "purge" ]; then
  userdel dumb-nomad
fi

if [ "$1" == "upgrade" ] && [ -d /run/systemd/system ]; then
  systemctl --system daemon-reload >/dev/null || true
  systemctl restart dumb-nomad >/dev/null || true
fi

exit 0
